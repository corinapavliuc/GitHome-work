Как скопировать на свой компьютер репозиторий из github.com, что должно быть сделано предварительно?
Для того чтобы скопировать репозиторий с GitHub на свой компьютер, вам понадобится:

1.Установить Git на свой компьютер. 

2.Создать учетную запись на GitHub (если у вас еще нет). 

3. Создать новый репозиторий на GitHub  или найти репозиторий, который вы хотите скопировать.

4. Получить URL-адрес репозитория. На странице репозитория на GitHub найдите кнопку "Clone" и скопируйте URL-адрес репозитория. URL-адрес будет выглядеть примерно так: `https://github.com/имя_пользователя/имя_репозитория.git`
После выполнения этих предварительных действий, вы готовы скопировать репозиторий на свой компьютер. Для этого выполните следующие шаги:

1. Откройте командную строку или терминал на своем компьютере.

2. Перейдите в каталог, в который вы хотите скопировать репозиторий. Например, для перехода в папку "Документы" вы можете использовать команду `cd Documents`.

3. В командной строке или терминале введите следующую команду:
git clone <URL-адрес репозитория>


Замените `` на фактический URL-адрес репозитория, который вы скопировали ранее.

4. Нажмите Enter, чтобы выполнить команду. Git скопирует репозиторий на ваш компьютер в указанный каталог.

Теперь вы можете работать с репозиторием на своем компьютере, делать изменения, коммитить их и пушить обратно на GitHub.


  
Как получить на локальный компьютер изменения с github.com?
  Для получения изменений с GitHub на ваш локальный компьютер, вам понадобится выполнить следующие шаги:

1. Откройте командную строку или терминал на своем компьютере.

2. Перейдите в каталог вашего локального репозитория. Например, для перехода в папку "Документы" вы можете использовать команду `cd Documents`.

3. В командной строке или терминале введите следующую команду:
git pull


Команда `git pull` извлекает изменения с удаленного репозитория (в данном случае с GitHub) и объединяет их с вашим локальным репозиторием.

4. Нажмите Enter, чтобы выполнить команду. Git извлечет изменения с GitHub и применит их к вашему локальному репозиторию.

Если в удаленном репозитории есть новые коммиты, Git автоматически применит эти изменения к вашему локальному репозиторию. Если возникнут конфликты, вам может потребоваться разрешить их вручную.

Теперь ваш локальный репозиторий будет содержать последние изменения с GitHub.




  Какую информацию нам дают команда :
git status
git log

Команда `git status` и `git log` предоставляют информацию о состоянии и истории вашего локального репозитория Git.

Команда `git status` показывает текущее состояние вашего репозитория, включая:

- Ветку, на которой вы находитесь.
- Измененные, непроиндексированные файлы.
- Индексированные, но не закоммиченные изменения.
- Новые файлы, которые еще не были добавлены в индекс.
- Файлы, которые были удалены, но еще не закоммичены.

Команда `git log` показывает историю коммитов в вашем репозитории, включая:

- Хэш каждого коммита.
- Автора и дату коммита.
- Сообщение коммита.

По умолчанию, `git log` показывает историю коммитов, начиная с самого последнего коммита и до самого первого. Вы можете использовать различные опции и флаги с `git log` для настройки вывода истории коммитов, например, 
  для показа только последних нескольких коммитов или для отображения изменений файлов в каждом коммите.


